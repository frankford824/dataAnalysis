# 虚构公司「山语茶室」。存在的唯一目的是证明引擎里没有藏公司知识。
#
# 它和真实的电商模型刻意做得不一样：平台不同、科目不同、利润口径只有 3 项而不是 11 项、
# 公式树只有 2 层而不是 4 层、关联键是会员单号而不是子订单编号。
# 如果这份模型能跑通，说明引擎确实只在解释模型数据。

id: demo-teahouse
name: 山语茶室（虚构，用于验证引擎通用性）
version: "1"
currency: CNY
